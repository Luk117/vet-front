/*
import axios from "axios";

export class ClientService{
    axios.get('/client', {
        params: {
          id: '1000265235'
        }
      })
    }).then(response => {
        this.user = response.data;
    }).catch(e => {
        console.log(e);
    })
}
*/